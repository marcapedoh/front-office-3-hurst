import { FormatSoldeRatePipe } from './format-solde-rate.pipe';

describe('FormatSoldeRatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatSoldeRatePipe();
    expect(pipe).toBeTruthy();
  });
});
