import { FormatMetaValuePipe } from './format-meta-value.pipe';

describe('FormatMetaValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatMetaValuePipe();
    expect(pipe).toBeTruthy();
  });
});
